[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sd212review"
authors = [
    {name = "Dan Roche", email = "roche@usna.edu"}
]
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
]
